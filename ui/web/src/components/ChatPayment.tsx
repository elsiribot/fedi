import React from 'react'

import { ChatMessage, ChatPayment as ChatPaymentType } from '@fedi/common/types'

interface Props {
    message: ChatMessage
    payment: ChatPaymentType
}

export const ChatPayment: React.FC<Props> = () => {
    // const isMe = mess

    return <div />
}
