# Fedi WASM

This contains the output from `bridge/fedi-wasm`. This will be replaced with a
proper build process and npm package, but for now the output is just checked in
here for to keep things simple.
